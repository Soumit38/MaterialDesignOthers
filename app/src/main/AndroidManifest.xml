<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soumit.materialdesignothers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MaterialProgressDialog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MaterialTexinputlayoutFeatures" />
        <activity android:name=".MaterialButtons" />
        <activity android:name=".MaterialSwitchButton" />
        <activity android:name=".MaterialCheckbox" />
        <activity android:name=".MaterialRadioButton" />
        <activity android:name=".MaterialProgressBar" />
        <activity android:name=".MaterialDialogs" />
        <activity android:name=".MaterialConfirmationDialog"></activity>
    </application>

</manifest>